import styles from './PendingData.module.css'

function PendingData() {
  return (
    <span className={styles.pending} aria-label="Dato pendiente">
      Dato pendiente
    </span>
  )
}

export default PendingData
